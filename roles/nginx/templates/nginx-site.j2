server {
       listen 80;
       listen 443 ssl;

       server_name heitto.vimpa.in;

       root /var/www/html/;
       index index.nginx-debian.html;

       location / {
               try_files $uri $uri/ =404;
       }
}
